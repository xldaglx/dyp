<div class="col-md-12 no-padding-m">
	<div class="deal-deta">
		<%= render 'users/header' %>
		<div class="col-md-3 no-padding">
		<%= render 'users/menuprofile' %>
		</div>
		<div class="col-md-9 contenedor-pub">
			<h1>Promociones Publicadas</h1>
			<% @users.each do |user| %>
			<p><%= user.nickname %> <%= user.rank %> <a href="">Seguir usuario</a> </p>
			<% end %>
		</div>
	</div>
</div>